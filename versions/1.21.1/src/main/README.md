Per-version overrides for Minecraft 1.21.1.

Files placed under `java/` or `resources/` here replace the file at the same relative path in
`common/src/main` for this build target only. 1.21.1 currently needs no overrides, so both are empty.
